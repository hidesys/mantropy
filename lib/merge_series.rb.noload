def ms(s0, s1)
  Serie.transaction do
    a = Serie.find(s0)
    b = Serie.find(s1)

    a.magazines << b.magazines
    a.books << b.books
    a.ranks << b.ranks

    a.save!
    b.destroy
  end
end

