順位,合計得点,糞補正後得点,ランキング重複数,糞重複数,コメント数,serie_id,name,漫画家,雑誌,ranks
<%- @series.each do |serie| -%>
  <%= serie.url[:rank] -%>,<%= serie.url[:sum_of_mark] -%>,<%= serie.url[:sum_of_mark_with_kuso] -%>,<%= serie.url[:count_rank] -%>,<%= serie.url[:count_kuso] -%>,<%= serie.url[:count_post] -%>,<%= serie.id -%>,<%= serie.name -%>,<%= serie.authors.map{|a| a.name}.join("　") -%>,<%= serie.magazines.select("DISTINCT *").map{|m| m.name}.join("　") -%>,<%= serie.ranks.where(:ranking_id => [5, 6]).sort{|a, b| (a.ranking_id <=> b.ranking_id).nonzero? || a.rank <=> b.rank}.map{|r| "#{r.user.name} (#{r.ranking_id == 6 ? "糞" : nil}#{r.rank}位)"}.join("　") %>
<%- end -%>
