<h1>Listing users</h1>

<table>
  <tr>
    <th>名前</th>
    <th>ランキング登録数</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.ranks.size %></td>
    <td><%=  %></td>
    <td><%=  %></td>
    <td><%=  %></td>
    <td><%=  %></td>
    <td><%= link_to 'Show', user_path(user.name) %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete if false %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
